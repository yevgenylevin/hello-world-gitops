apiVersion: v1
kind: Service
metadata:
  name: hello-world-canary
  namespace: hello-world-canary
spec:
  type: ClusterIP
  selector:
    app: hello-world-canary
  ports:
    - port: 80
      targetPort: 5000